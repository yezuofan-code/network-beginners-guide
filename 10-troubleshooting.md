# 10 故障排查：常见问题与解决方法

> ⏱ 适合人群：遇到问题的人
> 读完本文你将了解：常见问题怎么排查、用什么工具检测

---

## 一、最常见的问题和解决

### ❓ 问题：开了代理，但上不了网

**排查步骤**：

```
1. 检查订阅是否过期 → 去机场后台看
2. 换个节点试试 → 可能当前节点挂了
3. 重启客户端 → 有时候只是软件卡了
4. 检查系统代理是否开启 → Clash 里"系统代理"开关
5. 重启电脑/手机 → 终极办法
```

**大概率是**：节点挂了，换一个就行。

---

### ❓ 问题：速度很慢

**排查步骤**：

```
1. 测一下当前节点的延迟 → 客户端一般都有测速功能
2. 换一个节点 → 换到延迟低的
3. 换线路类型 → 普通线换专线节点（如果有）
4. 检查是不是在晚高峰 → 晚 8-11 点慢是正常的
5. 检查是不是在看 4K 视频 → 试试 1080p
```

**大概率是**：
- 你连的是普通线路节点，换专线节点
- 晚高峰拥堵，非高峰时段再用

---

### ❓ 问题：能连上但 YouTube 很卡

| 原因 | 解决方法 |
|------|---------|
| 节点速度不够 | 换一个速度更快的节点 |
| 线路拥堵 | 换专线节点或等非高峰 |
| 客户端设置了限制 | 检查 Clash 的"规则模式"是否正常 |
| 机场限速 | 去机场后台看是不是流量用完了 |

---

### ❓ 问题：淘宝/微信/银行 App 打不开

**原因**：你开了代理，国内 App 走了国际线路，反而变慢了。

**解决**：
1. 把客户端模式从「全局」改成「规则」
2. 如果已经是「规则」模式，更新一下规则列表
3. Clash Verge → 设置 → 更新 GEOSite 和 GEOIP

> 💡 **日常使用务必用「规则模式」**，国内网站走直连，国外走代理。

---

### ❓ 问题：iOS 无法连接

| 原因 | 解决方法 |
|------|---------|
| 订阅到期 | 去机场后台续费 |
| 客户端没更新 | 检查 Shadowrocket 有没有更新 |
| 节点不支持 | 换一个协议兼容的节点 |
| 配置问题 | 重新导入订阅链接 |

---

### ❓ 问题：TK 提示"网络环境不安全"

**原因**：TK 检测到你用的是机房 IP 或代理 IP。

**解决方法**：
1. 换住宅 IP 节点（如果机场有）
2. 用 LoongProxy 静态住宅 IP（推荐）
3. 检查 whoer.net 伪装度是否 ≥ 90%
4. 不要同时开 TK 和国内 App

---

### ❓ 问题：节点显示"超时"或"连接失败"

| 原因 | 解决方法 |
|------|---------|
| 节点被墙了 | 换一个节点，机场一般会自动换 IP |
| 端口被封 | 换一个端口 |
| 协议不兼容 | 换一个协议（Trojan 换 V2Ray） |
| 客户端版本太旧 | 更新到最新版 |

---

### ❓ 问题：自建 VPS 连不上

| 原因 | 解决方法 |
|------|---------|
| VPS IP 被墙 | 换 IP（阿里云可以换公网 IP） |
| 防火墙没关 | `ufw disable` 或开放端口 |
| WireGuard 没启动 | `systemctl status wg-quick@wg0` 查看 |
| 密钥不匹配 | 重新生成密钥对 |

---

## 二、常用检测工具

### 🌐 IP 检测

- [ipinfo.io](https://ipinfo.io) — 查看当前 IP 归属地和类型
- [whoer.net](https://whoer.net) — 检测伪装度（做 TK 必备）
- [ip.sb](https://ip.sb) — 简单显示 IP

### 📊 速度测试

- [fast.com](https://fast.com) — Netflix 出品，测速简单
- [speedtest.net](https://speedtest.net) — 经典测速
- 客户端自带的测速 — Clash Verge 等都有

### 🔍 路由检测

- `traceroute` / `tracert` — 看数据包走了哪些路由
- `ping` — 测延迟
- `mtr` — 连续检测路由质量（Windows 用 `winmtr`）

### 🔒 安全检测

- [dnsleaktest.com](https://dnsleaktest.com) — 检测 DNS 是否泄漏
- [ipleak.net](https://ipleak.net) — 综合泄漏检测

---

## 三、需要检查的常规项目

### 订阅是否到期

```
去机场网站登录 → 用户中心 → 查看剩余流量和到期时间
```

大部分客户端也会在界面上显示剩余流量。

### 客户端版本

```
Clash Verge → 设置 → 关于 → 检查更新
Shadowrocket → App Store 搜索 → 看有没有更新按钮
```

### 系统时间是否正确

代理协议对系统时间敏感，时间不对会导致连不上：

```
Windows: 设置 → 时间和语言 → 同步时间
Mac: 系统偏好设置 → 日期与时间 → 自动设置
iOS/Android: 设置 → 日期与时间 → 自动设置
```

---

## 四、什么时候该换服务商

如果你遇到以下情况，说明该换一家了：

| 信号 | 说明 |
|-----|------|
| 隔三差五连不上 | 服务商不稳定 |
| 晚高峰基本不能用 | 线路超售严重 |
| 节点越来越少 | 机场可能要跑路了 |
| 客服找不到人 | 联系不上就别续费了 |
| 速度越来越慢 | 可能换了线路或限速了 |

> 💡 **建议**：手里常备 2 个机场，一个主力一个备用，出问题随时切。

---

## 📌 本章小结

```
遇到问题 → 别慌，按顺序排查

1. 重启客户端 ✓
2. 换节点 ✓
3. 检查订阅是否到期 ✓
4. 检查系统时间 ✓
5. 重启设备 ✓
6. 换规则模式 ✓

还不行？找机场客服 → 还是不行？换一家。
```

---

[← 返回首页](./README.md)
