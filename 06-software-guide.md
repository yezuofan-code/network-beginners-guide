# 06 工具使用教程：到手后怎么配置

> ⏱ 适合人群：不会配置的人
> 读完本文你将了解：Clash Verge / Shadowrocket / v2rayN 的配置步骤

---

## 一、三个核心概念

在用任何客户端之前，先搞懂这三个词：

### 订阅链接

```
https://example.com/api/v1/client/subscribe?token=xxxxx
```

这是你买完机场后，在"用户中心"找到的一串网址。**它是你所有节点的"总列表"**。

### 导入订阅

把订阅链接粘贴到客户端里，客户端会自动把机场的所有节点下载到本地。

### 节点

就是一个个可以连接的服务器地址。一个机场通常有几十个节点，分布在不同的国家和地区。

---

## 二、Clash Verge 教程（推荐新手）

**适用平台**：Windows / macOS / Linux

### 第 1 步：下载安装

1. 访问 [GitHub Release 页](https://github.com/clash-verge-rev/clash-verge-rev/releases)
2. 下载最新版本：
   - Windows → 选 `Clash.Verge_x.x.x_x64-setup.exe` 或 `.msi`
   - macOS → 选 `Clash.Verge_x.x.x_aarch64.dmg`（Apple 芯片）或 `x64.dmg`（Intel 芯片）
3. 双击安装，一路下一步

### 第 2 步：导入订阅

```
打开 Clash Verge
    → 点左侧「订阅」
    → 在输入框粘贴你的订阅链接
    → 点「导入」
    → 软件会自动下载所有节点
```

### 第 3 步：选择节点

```
点「代理」标签
    → 你会看到很多节点分组（🇭🇰 香港 / 🇯🇵 日本 / 🇸🇬 新加坡...）
    → 点开一个地区，选一个节点
    → 选好后点「连接」
```

### 第 4 步：设置模式

Clash Verge 有几种模式：

| 模式 | 说明 | 什么时候用 |
|-----|------|-----------|
| **规则** | 国内网站直连，国外走代理 | ✅ **日常使用，推荐** |
| **全局** | 所有流量都走代理 | ⚠️ 看外网视频或特定情况 |
| **直连** | 不走代理 | ❌ 不需要代理时 |

> **日常用"规则模式"就够了**，上淘宝京东走直连，刷 YouTube 走代理，自动切换。

### 第 5 步：开启系统代理

```
点「设置」
    → 打开「系统代理」开关
    → 开关变蓝 = 开启成功
```

之后浏览器就能直接访问外网了。

### 常见问题

**Q：开了代理，淘宝/微信打不开？**
A：检查是不是误开了"全局模式"，改成"规则模式"就好了。

**Q：速度很慢？**
A：换一个节点试试，或者检查是不是走的普通线路节点。

**Q：Clash Verge 显示连接但打不开网页？**
A：试试重启软件，或者检查订阅是否过期。

---

## 三、Shadowrocket 教程（iOS 首选）

**适用平台**：iPhone / iPad
**价格**：¥15（App Store 买断）

### 第 1 步：购买下载

1. 打开 App Store
2. 搜索 "Shadowrocket"（小火箭图标）
3. 花 15 元购买下载

> ⚠️ **注意**：App Store 里 Shadowrocket 只有一个官方版本，不要下载其他山寨的。

### 第 2 步：导入订阅

```
打开 Shadowrocket
    → 点右上角「+」号
    → 类型选「Subscribe」（订阅）
    → URL 粘贴你的订阅链接
    → 点「完成」
    → 软件自动下载节点
```

### 第 3 步：选择节点

```
在节点列表里找到你想用的
    → 点一下节点 = 选中
    → 点顶部的「连接」开关
    → 开关变绿 = 连接成功
```

### 第 4 步：设置规则

```
点「配置」
    → 选择「Default」或你喜欢的规则集
    → 建议用默认规则（国内直连+国外代理）
```

### Shadowrocket 小技巧

- ✅ 长按节点可以测试延迟（ping）
- ✅ 左滑节点可以收藏（收藏的会排在前面）
- ✅ 点右上角扫码可以扫描二维码添加节点
- ✅ 下拉节点列表可以刷新订阅

---

## 四、v2rayN 教程（Windows 进阶）

**适用平台**：Windows
**适合人群**：习惯传统界面的人

### 第 1 步：下载

1. 访问 [GitHub Release 页](https://github.com/2dust/v2rayN/releases)
2. 下载 `v2rayN-With-Core.zip`（带核心文件的版本）
3. 解压到任意文件夹（**不要解压到桌面**，建议放 D 盘）

### 第 2 步：导入订阅

```
打开 v2rayN.exe
    → 点「服务器」→「订阅设置」
    → 点「添加」
    → URL 粘贴订阅链接
    → 备注写机场名字
    → 确定
    → 右键系统托盘的 v2rayN 图标
    → 「更新订阅」
```

### 第 3 步：选节点连接

```
在服务器列表双击一个节点
    → 节点前面出现 ✅ = 选中
    → 右键系统托盘的 v2rayN 图标
    → 开启「系统代理」
```

### 第 4 步：设置自动切换

```
点「路由」→「路由设置」
    → 勾选「启用路由功能」
    → 规则选「全局」或「按规则」
    → 推荐选「按规则」（国内直连+国外代理）
```

---

## 五、Clash Meta 教程（Android）

**适用平台**：Android

### 第 1 步：下载安装

1. 访问 [GitHub Release 页](https://github.com/MetaCubeX/ClashMetaForAndroid/releases)
2. 下载最新的 `.apk` 文件
3. 安装（如果提示"未知来源"，在设置里允许）

### 第 2 步：导入订阅

```
打开 Clash Meta
    → 点「配置」
    → 点右上角「+」
    → 选「URL」
    → 粘贴订阅链接
    → 点「下载」
    → 下载完成后点选这个配置
```

### 第 3 步：连接

```
回到首页
    → 点「启动」按钮
    → 提示"正在运行" = 成功
    → 点「代理」选节点
```

---

## 六、各软件界面通用操作

不管用哪个客户端，以下操作都是类似的：

| 操作 | 说明 |
|------|------|
| **订阅** | 买完机场第一步：导入订阅链接 |
| **更新订阅** | 机场新加了节点，点一下更新就能看到 |
| **测延迟** | 选节点前先测一下延迟，选绿的（延迟低） |
| **切换节点** | 一个节点卡了就换另一个，不要死磕 |
| **查看流量** | 大部分客户端能看到本月用了多少流量 |
| **更新版本** | 软件有新版本时，去 GitHub 下载更新 |

---

## 📌 本章小结

```
第一次使用

1. 买机场 → 拿到订阅链接
2. 下载客户端（Clash Verge 最推荐）
3. 粘贴订阅链接 → 导入
4. 选一个节点 → 连接
5. 模式选「规则」
6. 打开浏览器 → 可以上了 🎉
```

> 💡 **遇到问题**：先尝试换节点，再尝试重启客户端，大部分问题都能解决。

---

**下一章：[自建 VPS 指南：想自己搭服务器怎么买](./07-vps-guide.md)**

[← 返回首页](./README.md)
